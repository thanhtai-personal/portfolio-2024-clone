import { ReactNode } from "react";
import "./reset.css";
import "./index.css";
import "@ttt-ui/react-layout-mantine/dist/styles.css"

export const App = ({ children }: { children: ReactNode }) => {
  return children;
};
